'use client'
import {useState} from 'react'
import {createClient} from '@/lib/supabase/crew-client'
export function ProfileForm({id,name,phone}:{id:string;name:string;phone:string}){const [msg,setMsg]=useState(''),[busy,setBusy]=useState(false);return <form className="space-y-4" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const s=createClient();const {error}=await s.from('profiles').update({full_name:String(f.get('name')).trim(),phone_number:String(f.get('phone')).trim()}).eq('id',id);setMsg(error?'Opslaan mislukt.':'Profiel opgeslagen.');setBusy(false)}}><label className="block">Naam<input name="name" required maxLength={200} defaultValue={name} className="mt-1 block w-full rounded-xl border bg-background p-3"/></label><label className="block">Telefoon<input name="phone" type="tel" maxLength={40} defaultValue={phone} className="mt-1 block w-full rounded-xl border bg-background p-3"/></label><button disabled={busy} className="rounded-xl bg-violet-600 p-3">Opslaan</button><p role="status">{msg}</p></form>}
