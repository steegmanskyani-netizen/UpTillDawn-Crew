'use client'
/* eslint-disable @next/next/no-img-element */
import {useEffect,useMemo,useRef,useState} from 'react'
import {ChevronDown,FileText,Paperclip,Send,Users,X} from 'lucide-react'
import {createClient} from '@/lib/supabase/crew-client'
import {enqueue,enqueueChatPhoto} from '@/lib/crew-queue'
import type {Database,Tables} from '@/types/crew-database'
type CrewMember=Database['public']['Functions']['upt_crew_directory']['Returns'][number]
type Attachment={url:string;mimeType:string|null}
type ChannelCache={messages:Tables<'messages'>[];attachments:Record<string,Attachment[]>}
const ACCEPT='image/jpeg,image/png,image/webp,video/mp4,video/webm,video/quicktime,application/pdf,text/plain,text/csv,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.openxmlformats-officedocument.presentationml.presentation,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
function ChannelAvatar({src,size='lg'}:{src?:string;size?:'sm'|'lg'}){const classes=size==='lg'?'h-11 w-11':'h-9 w-9';return <div className={`flex ${classes} shrink-0 items-center justify-center overflow-hidden rounded-full border bg-muted`}>{src?<img src={src} alt="" className="h-full w-full object-cover"/>:<Users className={size==='lg'?'h-5 w-5':'h-4 w-4'}/>}</div>}
function AttachmentView({attachment,mine}:{attachment:Attachment;mine:boolean}){if(attachment.mimeType?.startsWith('video/'))return <video src={attachment.url} controls playsInline className="mt-2 max-h-80 w-full rounded-xl border border-white/15 bg-black"/>;if(attachment.mimeType?.startsWith('image/'))return <a href={attachment.url} target="_blank" rel="noreferrer" className="mt-2 block overflow-hidden rounded-xl border border-white/15"><img src={attachment.url} alt="Chatmedia" className="max-h-80 w-full object-contain bg-black/20"/></a>;return <a href={attachment.url} target="_blank" rel="noreferrer" className={`mt-2 flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${mine?'border-white/20 bg-white/10':'bg-muted/50'}`}><FileText className="h-5 w-5 shrink-0"/><span>Document openen</span></a>}
export function ChatClient({channels,defaultChannelId,userId,crewDirectory,isAdmin,profilePhotoUrls,channelImages}:{channels:Tables<'chat_channels'>[];defaultChannelId:string;userId:string;crewDirectory:CrewMember[];isAdmin:boolean;profilePhotoUrls:Record<string,string>;channelImages:Record<string,string>}){
 const [selected,setSelected]=useState(defaultChannelId||channels[0]?.id||''),[cache,setCache]=useState<Record<string,ChannelCache>>({}),[body,setBody]=useState(''),[file,setFile]=useState<File|null>(null),[fileKey,setFileKey]=useState(0),[status,setStatus]=useState(''),[busy,setBusy]=useState(false),[pickerOpen,setPickerOpen]=useState(false);const endRef=useRef<HTMLDivElement>(null);const effectiveSelected=selected&&channels.some(c=>c.id===selected)?selected:defaultChannelId||channels[0]?.id||'',current=cache[effectiveSelected]||{messages:[],attachments:{}},selectedChannel=channels.find(c=>c.id===effectiveSelected)
 useEffect(()=>{if(!effectiveSelected)return;const s=createClient();let alive=true;async function load(){const {data,error}=await s.from('messages').select('*').eq('channel_id',effectiveSelected).order('created_at',{ascending:false}).limit(100);if(!alive)return;if(error){setStatus('Berichten konden niet worden geladen.');return}const ordered=(data||[]).reverse(),ids=ordered.map(m=>m.id),grouped:Record<string,Attachment[]>={};if(ids.length){const {data:rows}=await s.from('message_attachments').select('message_id,storage_path,mime_type').in('message_id',ids);const signed=await Promise.all((rows||[]).filter(r=>r.storage_path).map(async r=>{const {data:url}=await s.storage.from('chat-attachments').createSignedUrl(r.storage_path!,300);return url?.signedUrl?{messageId:r.message_id,url:url.signedUrl,mimeType:r.mime_type}:null}));for(const item of signed){if(item)(grouped[item.messageId]||=[]).push({url:item.url,mimeType:item.mimeType})}}if(alive)setCache(p=>({...p,[effectiveSelected]:{messages:ordered,attachments:grouped}}))}void load();const channel=s.channel(`crew-chat-${effectiveSelected}`).on('postgres_changes',{event:'*',schema:'public',table:'messages',filter:`channel_id=eq.${effectiveSelected}`},()=>void load()).subscribe(),timer=window.setInterval(()=>void load(),10000);return()=>{alive=false;window.clearInterval(timer);void s.removeChannel(channel)}},[effectiveSelected]);useEffect(()=>{endRef.current?.scrollIntoView({block:'end'})},[current.messages.length,effectiveSelected]);const directory=useMemo(()=>new Map(crewDirectory.map(m=>[m.id,m])),[crewDirectory]);const channelName=(c:Tables<'chat_channels'>)=>c.kind==='organization'?'Algemene chat':c.kind==='workplace'?(c.name||'Werkplekchat'):(c.name||'Eventchat'),eventChannels=channels.filter(c=>c.kind==='event'),workplaceChannels=channels.filter(c=>c.kind==='workplace'),organizationChannels=channels.filter(c=>c.kind==='organization');function chooseChannel(id:string){setSelected(id);setPickerOpen(false);setStatus('')}
 async function sendMessage(){const trimmed=body.trim();if(busy||!effectiveSelected||(!trimmed&&!file))return;setBusy(true);setStatus('');try{if(file){await enqueueChatPhoto(userId,effectiveSelected,trimmed,file);setBody('');setFile(null);setFileKey(k=>k+1);setStatus(navigator.onLine?'Bestand en bericht zijn bewaard voor serververwerking.':'Bestand en bericht zijn lokaal bewaard en worden verzonden zodra je online bent.')}else{await enqueue(userId,'message',{channel_id:effectiveSelected,body:trimmed});setBody('')}}catch(error){setStatus(error instanceof Error?error.message:'Bericht kon niet worden bewaard.')}finally{setBusy(false)}}async function moderate(messageId:string){const reason=window.prompt('Reden voor moderatie (wordt geaudit):')?.trim();if(!reason||busy)return;setBusy(true);try{const s=createClient(),{error}=await s.rpc('upt_moderate_message',{p_message:messageId,p_reason:reason});if(error)throw error;setStatus('Bericht gemodereerd.')}catch{setStatus('Moderatie mislukt.')}finally{setBusy(false)}}
 if(!selectedChannel)return <section className="rounded-2xl border p-6"><h1 className="text-2xl font-black">Chat</h1><p className="mt-2 text-muted-foreground">Er zijn momenteel geen beschikbare chats.</p></section>
 return <section className="relative flex min-h-[calc(100dvh-9rem)] flex-col overflow-hidden bg-background md:min-h-[70vh] md:rounded-3xl md:border"><header className="sticky top-0 z-30 border-b bg-background/95 px-3 py-3 backdrop-blur md:px-4"><button type="button" onClick={()=>setPickerOpen(o=>!o)} className="flex w-full items-center gap-3 rounded-2xl p-1 text-left hover:bg-muted/60"><ChannelAvatar src={channelImages[selectedChannel.id]}/><div className="min-w-0 flex-1"><p className="truncate text-base font-black">{channelName(selectedChannel)}</p><p className="text-xs text-muted-foreground">Tik om van chat te wisselen</p></div><ChevronDown className={`h-5 w-5 ${pickerOpen?'rotate-180':''}`}/></button>{pickerOpen&&<div className="absolute left-3 right-3 top-[4.5rem] z-40 max-h-[65vh] overflow-y-auto rounded-2xl border bg-card p-2 shadow-2xl"><div className="flex items-center justify-between px-2 py-1"><p className="text-sm font-black">Gesprekken</p><button type="button" onClick={()=>setPickerOpen(false)} className="rounded-full p-2"><X className="h-4 w-4"/></button></div>{[[organizationChannels,'Algemeen'],[eventChannels,'Evenementen'],[workplaceChannels,'Werkplekken']].map(([group,label])=><div key={label as string} className="mt-2"><p className="px-2 py-1 text-[11px] font-bold uppercase tracking-[.16em] text-muted-foreground">{label as string}</p>{(group as Tables<'chat_channels'>[]).map(c=><ChannelButton key={c.id} channel={c} selected={effectiveSelected} onChoose={chooseChannel} name={channelName(c)} image={channelImages[c.id]}/>)}</div>)}</div>}</header><div className="flex-1 space-y-3 overflow-y-auto px-3 py-4 md:px-5">{!current.messages.length&&<p className="py-10 text-center text-sm text-muted-foreground">Nog geen berichten in deze chat.</p>}{current.messages.map(message=>{const sender=message.sender_id?directory.get(message.sender_id):undefined,mine=message.sender_id===userId,moderated=Boolean(message.moderated_at),senderName=mine?'Jij':sender?.full_name||'Personeelslid',initials=(mine?(directory.get(userId)?.full_name||'Jij'):senderName).split(/\s+/).map(p=>p[0]).join('').slice(0,2).toUpperCase(),photoUrl=message.sender_id?profilePhotoUrls[message.sender_id]:undefined,timestamp=selectedChannel.kind==='organization'?new Date(message.created_at).toLocaleString('nl-BE'):new Date(message.created_at).toLocaleTimeString('nl-BE',{hour:'2-digit',minute:'2-digit'});return <article key={message.id} className={`flex items-end gap-2 ${mine?'justify-end':'justify-start'}`}>{!mine&&<div className="h-8 w-8 shrink-0 overflow-hidden rounded-full border bg-muted">{photoUrl?<img src={photoUrl} alt="" className="h-full w-full object-cover"/>:<div className="flex h-full w-full items-center justify-center text-[10px] font-black">{initials}</div>}</div>}<div className={`max-w-[82%] ${mine?'items-end':'items-start'} flex flex-col`}>{!mine&&<p className="mb-1 px-1 text-xs font-bold text-muted-foreground">{senderName}</p>}<div className={`rounded-2xl px-3.5 py-2.5 shadow-sm ${mine?'rounded-br-md bg-violet-600 text-white':'rounded-bl-md border bg-card'}`}><p className="whitespace-pre-wrap break-words text-sm leading-relaxed">{moderated?'Bericht verwijderd door beheerder':message.body}</p>{!moderated&&current.attachments[message.id]?.map((a,i)=><AttachmentView key={i} attachment={a} mine={mine}/>)}<div className={`mt-1.5 flex items-center justify-end gap-2 text-[10px] ${mine?'text-white/75':'text-muted-foreground'}`}><time>{timestamp}</time>{isAdmin&&!moderated&&<button type="button" disabled={busy} onClick={()=>moderate(message.id)} className="underline">Modereer</button>}</div></div></div></article>})}<div ref={endRef}/></div><div className="sticky bottom-0 z-20 border-t bg-background/95 p-3 backdrop-blur">{file&&<div className="mb-2 flex items-center justify-between rounded-xl border bg-card px-3 py-2 text-xs"><span className="truncate">{file.name}</span><button type="button" onClick={()=>{setFile(null);setFileKey(k=>k+1)}}><X className="h-4 w-4"/></button></div>}<form className="flex items-end gap-2" onSubmit={e=>{e.preventDefault();void sendMessage()}}><label className="flex h-11 w-11 shrink-0 cursor-pointer items-center justify-center rounded-full border bg-card" aria-label="Bestand toevoegen"><Paperclip className="h-5 w-5"/><input key={fileKey} type="file" accept={ACCEPT} onChange={e=>setFile(e.target.files?.[0]||null)} className="sr-only"/></label><textarea maxLength={4000} rows={2} value={body} onChange={e=>setBody(e.target.value)} placeholder="Typ een bericht…" className="max-h-32 min-h-11 min-w-0 flex-1 resize-none rounded-2xl border bg-card px-4 py-3 text-sm"/><button type="submit" disabled={busy||(!body.trim()&&!file)} className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-violet-600 text-white disabled:opacity-40"><Send className="h-5 w-5"/></button></form><p className="mt-1 text-center text-[10px] text-muted-foreground">Enter = nieuwe regel · verzenden gebeurt met de knop.</p>{status&&<p className="mt-2 text-center text-xs text-muted-foreground">{status}</p>}</div></section>}
function ChannelButton({channel,selected,onChoose,name,image}:{channel:Tables<'chat_channels'>;selected:string;onChoose:(id:string)=>void;name:string;image?:string}){return <button type="button" onClick={()=>onChoose(channel.id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2 text-left ${channel.id===selected?'bg-violet-600 text-white':'hover:bg-muted'}`}><ChannelAvatar src={image} size="sm"/><span className="min-w-0 flex-1 truncate text-sm font-bold">{name}</span></button>}
