import { createClient } from '@/lib/supabase/crew-server'
export const dynamic='force-dynamic'
export default async function Page(){const s=await createClient();const {data,error}=await s.from('crew_notifications').select('id,title,body,created_at').order('created_at',{ascending:false}).limit(100);return <main className="space-y-4 p-4 md:p-8"><h1 className="text-3xl font-black">Meldingen</h1>{error?<p>Meldingen konden niet worden geladen.</p>:!data?.length?<p>Geen meldingen.</p>:data.map(n=><article key={n.id} className="rounded-xl border p-4"><h2 className="font-bold">{n.title}</h2><p>{new Date(n.created_at).toLocaleString('nl-BE')}</p></article>)}</main>}
