import { createClient } from "@/lib/supabase/server"
export const dynamic="force-dynamic"
export default async function Page(){const s:any=await createClient();const{data,error}=await s.from("incidents").select("message,status,created_at").limit(100);return <div className="p-4 md:p-8"><p className="text-violet-400 text-xs font-bold tracking-[.2em]">UPTILLDAWN</p><h1 className="text-3xl font-black mt-1">Incidents</h1><p className="text-muted-foreground mb-6">URGENT meldingen en operationele incidenten.</p>{error?<div className="rounded-xl border border-amber-500/30 bg-amber-500/10 p-4 text-sm">Database migration 026 moet nog in Supabase worden uitgevoerd.</div>:<div className="grid gap-3">{data?.length?data.map((x:any,i:number)=><div key={x.id||i} className="rounded-2xl border border-border bg-card p-4"><pre className="whitespace-pre-wrap text-sm">{JSON.stringify(x,null,2)}</pre></div>):<div className="rounded-2xl border border-dashed p-8 text-center text-muted-foreground">Nog geen gegevens.</div>}</div>}</div>}
