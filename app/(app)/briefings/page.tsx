import {createClient} from '@/lib/supabase/crew-server'
import {getCurrentUser} from '@/lib/actions/auth'
import {acknowledgeBriefing,acknowledgeInstruction,createBriefing} from '@/lib/actions/uptilldawn'
export default async function Page(){const s=await createClient();const user=await getCurrentUser();if(!user)return null;const [{data:briefs,error},{data:personal},{data:acks},{data:packs},{data:events}]=await Promise.all([s.from('briefings').select('*').order('created_at',{ascending:false}),s.from('personal_instructions').select('*').eq('user_id',user.id),s.from('briefing_acknowledgements').select('*').eq('user_id',user.id),s.from('personal_instruction_acknowledgements').select('*').eq('user_id',user.id),s.from('events').select('id,name')]);return <main className="space-y-5 p-4 md:p-8"><h1 className="text-3xl font-black">Briefings & instructies</h1>{user.isAdmin&&<form action={createBriefing} className="grid gap-3 rounded-xl border p-4"><select name="event_id" required className="border bg-background p-3">{events?.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select><input name="title" required placeholder="Titel" className="border bg-background p-3"/><textarea name="body" required placeholder="Algemene briefing" className="min-h-28 border bg-background p-3"/><button className="rounded-xl bg-violet-600 p-3">Briefing aanmaken</button></form>}{error&&<p>Briefings konden niet worden geladen.</p>}{briefs?.map(b=><article key={b.id} className="space-y-3 rounded-xl border p-4"><h2 className="text-xl font-bold">{b.title} · v{b.version}</h2><p className="whitespace-pre-wrap">{b.body}</p>{acks?.some(a=>a.briefing_id===b.id&&a.version===b.version)?<p>BRIEFING READ</p>:<form action={acknowledgeBriefing}><input type="hidden" name="id" value={b.id}/><button className="rounded-xl border p-3">BRIEFING READ</button></form>}</article>)}{personal?.map(b=><article key={b.id} className="space-y-3 rounded-xl border border-violet-500 p-4"><h2 className="text-xl font-bold">Persoonlijk: {b.title} · v{b.version}</h2><p className="whitespace-pre-wrap">{b.body}</p>{packs?.some(a=>a.instruction_id===b.id&&a.version===b.version)?<p>Gelezen</p>:<form action={acknowledgeInstruction}><input type="hidden" name="id" value={b.id}/><button className="rounded-xl border p-3">Gelezen bevestigen</button></form>}</article>)}</main>}
